# cleyes

Real-time webcam visual effect that dissolves your camera feed into separated RGB pixel clouds using WebGL 2.0 fragment shaders.

## Requirements

- Node.js >= 18
- A browser with WebGL 2.0 support
- Webcam

## Quick Start

```bash
npm install
npm run dev
```
